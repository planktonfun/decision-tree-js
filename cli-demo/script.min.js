var desobj=require("./decision-tree.js"),fs=require("fs"),dt=desobj.dt,basepath="json_files/",jsonContent=fs.readFileSync(basepath+"config.json","utf8"),config=JSON.parse(jsonContent),decisionTree=new dt.DecisionTree(config),numberOfTrees=3,randomForest=new dt.RandomForest(config,numberOfTrees),jsonContent=fs.readFileSync(basepath+"input.json","utf8"),comic=JSON.parse(jsonContent),decisionTreePrediction=decisionTree.predict(comic),randomForestPrediction=randomForest.predict(comic);
console.log(JSON.stringify(comic,null,0));console.log(JSON.stringify(decisionTreePrediction,null,0));console.log(JSON.stringify(randomForestPrediction,null,0));console.log(treeToHtml(decisionTree.root));function treeToHtml(a){if(a.category)return[' "',a.category,'"'].join("");var b={},c=['"',a.attribute," ",a.predicateName," ",a.pivot,' ?"'].join("");b[c]={yes:treeToHtml(a.match),no:treeToHtml(a.notMatch)};return b};
